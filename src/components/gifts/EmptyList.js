import React from 'react';
import { Empty } from '../styles/gifts/list/List.styled';

export const EmptyList = () => {
    return (
        <Empty>There is no gifts in your list. Please start adding some!</Empty>
    )
}